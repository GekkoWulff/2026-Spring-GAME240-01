//initial guess 

int currentNumber;

currentNumber = 1;

while (currentNumber <= 100)
{
    Console.WriteLine(currentNumber);
    currentNumber = currentNumber + 1;
}

/* 
1. Ask user to play the game
2. Take the guess
3. Ask the user if it's correct

While (we are not right) 
{
    come up with new number
    tell user new guess 
    ask if this is right
}

add together and divide by 2 to find average
50 -> 75 -> 62 -> 68

write message saying we got it right 

*/
